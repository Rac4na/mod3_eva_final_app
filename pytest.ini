[pytest]
# Sin esto pytest solo agrega tests/ a sys.path (no hay __init__.py ahí) y el
# `from app.main import ...` falla con ModuleNotFoundError dentro del contenedor.
pythonpath = .
testpaths = tests
